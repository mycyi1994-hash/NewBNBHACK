/**
 * Pure domain logic, no I/O (SPEC §4–§6): plan and cycle types, decideCycle(), guardian rules and
 * amount maths. Filled by M0-07 (amounts), M1-01 (types) and M1-02 (decideCycle).
 */
export * from './amounts.js';
export * from './session.js';
